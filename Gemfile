# frozen_string_literal: true

source "https://rubygems.org"
ruby "3.3.0"

# Core gems
gem "httparty"      # For Ollama API calls
gem "json"          # JSON handling
gem "ffi"           # For C library bindings

# Audio processing - removed ruby-audio due to libsndfile issues
# We'll use system calls to sox/ffmpeg instead

# Development and debugging
gem "pry"           # Debugging
gem "rspec"         # Testing
